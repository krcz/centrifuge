# Store Synchronization

These fixtures are used to verify store pull/push synchronization for imported
oxide graphs.

- `fischer-esterification-bench.yaml`
  Direct YAML fixture for a Fischer esterification bench setup. Use
  `schema/reaction-bench.schema.yamlld`.
