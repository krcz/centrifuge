# Solvent / Export

These fixtures are used to verify that imported values can be re-hydrated into a
`Solvent`, persisted, and exported again.

- `aldol-bench.yaml`
  Direct YAML fixture for an aldol reaction bench setup. Use
  `schema/reaction-bench.schema.yamlld`.
